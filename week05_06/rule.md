| 순번  | 규칙         | 내용                  |
|-----|------------|---------------------|
| `1` | 테스트 단위 기준  | 각 도메인의 public 메서드   |
| `2` | 테스트 제외 기준  | 단순 조회, IO           |
| `3` | 테스트 난항 대응  | 기능의 분리와 추출          |
| `4` | 리팩터링 우선 순위 | 컨벤션 > 책임 분리 > 기능 개선 |

1. 테스트 단위 기준 - 각 도메인의 public 메서드

> 모든 말단(private) 메소드를 테스트하기 위해 public 으로 접근 제어를 열어놓는 건 최악의 안티 패턴
> 도메인의 행위 를 테스트, 결과로 유효성 검증
> 상태 를 산출(조회)하는 행위도 포함.

도메인의 행위를 테스트를 통해 검증함으로써 완결성, 자율성, 책임 분리를 검증.

---

2. 테스트 제외 기준 - 단순 상태 조회, IO 메서드

> IO 로직은 도메인의 핵심 비즈니스 로직이 아니다. 테스트 제외 대상
> 단순 getter 등의 상태 조회는 그 호출부에 대한 검증으로 대체

통합적인 기능의 결과를 확인하는 것은  
통합 테스트 단에서 한 번에 검증.

---

3. 테스트 어려움 대응 - 기능의 분리와 추출

> 기능이 세부적으로 작게 분리되었다면
> 테스트가 어려울 수가 없다
> 도메인의 책임이 제대로 분리되었다면
> 테스트가 어려울 수가 없다

명확한 기준에 맞춰 책임을 분리하고 역할 이관

---

4. 리팩터링 우선 순위

> 컨벤션 > 책임+역할 분리 > 기능 개선

규칙이 곧 역할 분리와 단일 책임을 위한 기반이라 생각.  
크루 > 코스 > 마을 > 조 > 페어 간 협업을 위한 컨벤션을 우선해서 맞추고  
이후 순서대로.

---
