# ♠️ 미션

<details><summary><h2>미션</h2></summary>



</details>

---

# `4. 피드백 활동`

피드백 활동

캐리: 2번 잘 작동. 1번은 추상적이다 보니 적용하기 어려움
시간/테마/예약에 대해 각각이 독립적인 리소스인지 여부에 따라서인데
그런 내용이 정해져있진 않아서 규칙을 적용하기 힘들었고,
단순한 리소스의 정의는 내 관점으로 바로바로 가능했지만
복잡한 리소스, 여러 스코프로 요청하는 엔드포인트를 설계하는 데엔 규칙이랄게 애매

기본적인 / 단순한 엔드포인트는 심플하고 의견의 상충이 없지만
복합적인 엔드포인트 설계 기준에 대한 규칙이 있는것이 좋지 않을까
설계한 방식은 /theme/time?date= 구조.
변경 가능성에 따라 리소스를 식별하고 path 에 활용하는 것이?

캐리의 결론 : 리소스의 독립성/종속성이 API 설계를 결정하는 기준

대길 리뷰어 : 고유 식별자만이 리소스이기에 path 로, 나머지는 parameter 로.

좋은 API

대길 : 명확하고 직관적인 API. 뚜렷하게 알아볼 수 있는 API
캐리 : 응답을 기대할 수 있고 일관된 규칙을 가진 API
마이찬 : 이해하기 쉬운 API. 어떻게 작성하는지보단 그 결과가 명확히 이해하기 쉬운지
어떻게, 행위는 숨기고 무엇을 하는지를 드러낸다
글렌 : 엔드포인트는 무엇을, 메서드를 어떻게 를 표현하고 상호 보완하고 상호 필수적인 API

대길 : 명확성
캐리 : 일관성
마이찬 : 은닉성
글렌 : 상호보완성

마이찬 예외 응답

1. 예외 응답에 어떤 데이터를 넣어야 할까요?

현재 구조
오류가 발생했을 때 클라이언트에게 아래 형식으로 응답하고 있습니다.

{
"httpStatus": "NOT_FOUND",
"errorMessage": "예약이 존재하지 않습니다.",
"timeStamp": "2026-05-13T21:00:00",
"apiUrl": "/reservations/99",
"traceId": "a1b2-c3d4-..."
}

각 필드를 선택한 이유는 다음과 같습니다.

필드
넣은 이유

httpStatus
클라이언트가 상태 코드를 body에서도 확인할 수 있도록

errorMessage
어떤 문제인지 사람이 읽을 수 있게

timeStamp
언제 오류가 발생했는지 추적을 위해

apiUrl
어느 경로에서 발생했는지 파악하기 위해

traceId
요청마다 UUID를 발급해 로그와 연결하기 위해

traceId는 모든 요청 시작 시 Filter에서 UUID를 만들어 MDC에 저장해두고, 예외 응답 시 꺼내 씁니다.

// TraceIdFilter.java
MDC.put("traceId", UUID.randomUUID().toString());chain.doFilter(request, response);MDC.clear();

1. 관리자/사용자 API 분리 기준

같은 자원을 다루는 관리자와 사용자 API를 분리하는가, 합치는가

* (If-Then) 만약 리소스의 도메인 모델과 생성 행위가 동일하고, 사용자 권한이나 사용 맥락에 따른 정책 차이가 없다면 → 관리자와 사용자 구분 없이 같은 엔드포인트를 사용할 수 있다.
    * 예: 누가 생성하든 동일한 정책으로 처리되는 예약 생성
* (If-Then) 만약 리소스의 식별자는 동일하지만, 사용자 권한이나 사용 맥락(Context)에 따라 정책과 허용 범위가 달라진다면 → 관리자와 사용자 API를 분리하거나 정책 로직을 별도로 둔다.
    * 예: 사용자는 과거 예약 생성이 제한되지만, 관리자는 현장 상황상 과거 예약을 대신 등록할 수 있다.
* (If-Then) 만약 사용자의 권한에 따라 응답 필드의 구성이나 노출 데이터가 현저히 다르다면 → 도메인 목적에 따라 API를 분리하거나 권한 로직으로 분기한다.
* (우선순위) 결정 순서

    1. 리소스의 식별자가 동일한가?
    2. 조회/수정의 목적과 Context가 동일한가?
    3. 사용자 권한이나 사용 맥락에 따라 정책과 허용 범위가 달라지는가?


5. 에러 응답 수신자/내용

에러 응답의 직접 수신자는 이 API를 사용하는 클라이언트다.

* 클라이언트는 웹 UI, 서버, 외부 API 소비자, 개발자 등 API를 호출하는 주체가 될 수 있다.
* 서버는 클라이언트가 요청 실패 원인을 이해하고 다음 처리를 판단할 수 있도록 일관된 정보를 제공한다.
* 최종 사용자에게 어떤 문구를 보여줄지는 클라이언트가 결정할 수 있다.

실패하면 사용자에게 뭘 보여줘야 하는가?

글렌 : 실패의 내용, 실패의 원인, 실패의 대응을 요청-실패-응답 과정 내에서만 뽑아내서 보여줘야 한다

좋은 에러 응답의 조건

사용자가 실패의 내용을 알 수 있다
사용자가 실패의 대응을 알 수 있다

규칙 중 도움
1번 리소스가 명확히 식별되어 있기에 서버도 클라이언트도 원인을 명확히 할 수 있다
2번 서버에 존재하는 원천 데이터를 서버가 검증하기에 실패의 응답에 명확히 담을 수 있다
4번 엔드포인트가 무엇을, 메서드가 어떻게를 표현하고 명확한 요청에 적절한 응답을 담을 수 있다
5번 응답을 받을 대상과 그를 위한 내용을 적절히 담아 응답할 수 있다
6번 응답에 담을 내용의 범위와 그를 위한 행위를 적절히 제한해 응답할 수 있다

에러 응답에 대한 생각

지금의 생각

변화의 이유

에러 상황에서도 유지된 것, 수정/폐기

가져가고 싶은 규칙, 만족시키는 조건

대길
에러가 발생한 원인과 다음 행동을 보여준다.
→ 사용자가 원인만 보고 다시 한 번 더 생각하는 것을 방지

캐리

* 상태 코드와 함께, 클라이언트가 실패 원인을 안정적으로 식별할 수 있는 error code와 원인 이해를 위한 message를 제공한다.
* error code는 클라이언트가 분기하기 위한 안정적인 계약으로 사용한다.
* message는 클라이언트가 다음 처리를 판단할 수 있는 단서를 제공한다.
* 서버가 최종 사용자에게 특정 행동을 직접 지시하는 UX 문구를 강제하지 않는다.

캐리
상태 코드와 함께, 클라이언트가 실패 원인을 안정적으로 식별할 수 있는 code와 원인 이해를 위한 message를 제공한다.

* code는 클라이언트가 분기하기 위한 안정적인 계약으로 사용한다.
* message는 클라이언트가 다음 처리를 판단할 수 있는 단서를 제공한다.
* 서버가 최종 사용자에게 특정 행동을 직접 지시하는 UX 문구를 강제하지

사이클2 - 최종 규칙

1. 리소스 식별 기준

URL을 정할 때 무엇을 리소스로 보고, 무엇을 필터·표현 조건으로 보는가

Rule 1. 도메인 분석을 통해, 실행 흐름에 나타나는 명사를 리소스로 식별한다.
리소스 식별 규칙 자체는 추상적으로 설정하되,
API 명세 설계에 대한 각자의 관점에 맞춰 판단하고

* 서버/도메인 모델 관점
* 클라이언트/멘탈 모델 관점

이후 피드백 활동에서 각자의 경험을 공유해 규칙을 구체화한다.

2. 서버/클라이언트 책임 기준

어떤 판단을 서버가 책임지고, 어떤 판단을 클라이언트가 책임지는가

* (If-Then) 만약 판단에 필요한 원천 데이터가 서버에만 존재한다면 → 해당 로직은 서버가 책임지고 결과값을 내려준다.
    * 예: "예약 가능 여부(available)"는 서버가 기존 예약 현황을 계산하여 응답에 포함한다.

* (If-Then) 만약 클라이언트가 받은 데이터를 가공 없이 화면에 그리는 역할이라면 → 서버는 클라이언트가 바로 사용할 수 있는 형태(UI friendly)로 응답을 설계한다.

* (우선순위) 책임 판단 순서:

    1. 데이터의 무결성을 보장해야 하는 곳이 어디인가? (보통 서버)
    2. 클라이언트가 중복된 계산 로직을 가질 필요가 있는가?

* (금지) 클라이언트에게 비즈니스 로직 판단을 떠넘기지 않는다.
    * 이유: 데이터 불일치 문제를 방지하고 클라이언트의 복잡도를 낮추기 위함이다.


3. 관리자/사용자 API 분리 기준

같은 자원을 다루는 관리자와 사용자 API를 분리하는가, 합치는가

* (If-Then) 만약 리소스의 도메인 모델과 생성 행위가 동일하다면 → 관리자와 사용자 구분 없이 같은 엔드포인트를 사용한다.
    * 예: POST /reservations (누가 생성하든 '예약 생성'이라는 행위는 동일함)
* (If-Then) 만약 사용자의 권한에 따라 응답 필드의 구성이나 노출 데이터가 현저히 다르다면 → 도메인 목적에 따라 API를 분리하거나 권한 로직으로 분기한다.
* (우선순위) 결정 순서:

    1. 리소스의 식별자가 동일한가?
    2. 조회/수정의 목적과 Context가 동일한가?


4. 우리 그룹의 "좋은 API" 정의

동작하는 것을 넘어, 어떤 조건을 만족해야 좋은 API라고 부를 것인가

좋은 API

* 정의: 동작을 완벽히 보장하는 것은 물론, 뛰어난 가독성을 바탕으로 명세만 보아도 요청과 행동을 직관적으로 식별할 수 있는 API.
* 특징:
    * 직관성: URL(리소스 명사)과 HTTP 메서드(행위 동사)의 조합만으로 해당 API가 어떤 자원에 대해 무슨 작업을 수행하는지 누구나 명확히 추론할 수 있습니다.
    * 명확성: 리소스의 정의, 메서드 시그니처 등에 대해 팀이 합의한 명확한 기준과 규칙을 엄격하게 따릅니다.
    * 문서화의 역할: 잘 짜인 API 명세 자체가 훌륭한 설명서 역할을 하여, 개발자 간의 소통 비용과 유지보수 비용을 크게 낮춥니다.


5. 에러 응답 수신자/내용

1. 에러 응답 수신자/내용**
2. 에러 응답의 직접 수신자는 API를 호출하는 클라이언트다.
3. 클라이언트는 웹 UI, 모바일 앱, 서버, 외부 API 소비자 등 API를 사용하는 모든 주체를 포함한다.
4. 에러 응답은 클라이언트가 실패 원인을 식별하고 후속 처리를 판단할 수 있도록 일관된 정보를 제공해야 한다.

응답 내용은 다음 정보를 핵심으로 한다.

* code
    * 클라이언트 분기 처리를 위한 안정적인 식별 값
* message
    * 실패 원인을 이해할 수 있도록 설명하는 메시지
      예시:

{
"code": "INVALID_INPUT",
"message": "email 형식이 올바르지 않습니다."
}

6. 비즈니스 규칙 위반 시 응답과 응답에 “다음 행동"을 담는 기준

* (If-Then) 만약 비즈니스 규칙을 위반한 요청이라면
    * → 에러 발생 원인과 다음 행동을 응답에 포함한다.
* (금지) 다음 행동에는 민감한 정보 (비밀번호, 계좌번호 등)를 포함하지 않는다.
    * (예시) "비밀번호의 3번째자리가 틀렸습니다.

